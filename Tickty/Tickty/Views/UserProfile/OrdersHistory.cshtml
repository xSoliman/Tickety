@* @model List<LapZone.Models.Order>

<style>
    .table-container {
        margin-bottom: 20px;
        margin-right: 50px;
        margin-left: 50px;
    }

    .table-responsive {
        border: 1px solid #ddd;
        border-radius: 8px;
        overflow-x: auto;
    }

    table.table {
        width: 100%;
        max-width: 100%;
        margin-bottom: 1rem;
        background-color: #fff;
        border-collapse: collapse;
        border: 1px solid #ddd;
        border-radius: 8px;
    }

        table.table th,
        table.table td {
            padding: 12px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }

        table.table th {
            background-color: #1e1f29;
            color: #fff;
        }

        table.table tbody tr:hover {
            background-color: #f5f5f5;
        }

    .order-details-button {
        background-color: #d10024;
        font-weight: bold;
        color: #fff;
        padding-right:10px ;
        padding-left: 10px;
        padding-top: 5px;
        padding-bottom: 5px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

        .order-details-button:hover {
            background-color: #1e1f29;
        }

    .my-prfile-link {
        color: #d10024;
      /*   padding: 10px 15px; */
        border: none;
        text-decoration: none;
        font-size: 4rem;
        margin-top: 3rem;
        margin-right: -60px;
        margin-left: 55px;
        font-weight: bold; 
        transition: background-color 0.3s, color 0.3s;
    }

        .my-prfile-link:hover {
            /* background-color: #1e1f29; */
            color: #1e1f29;
        }
</style>
<a asp-controller="UserProfile" asp-action="Index" class="my-prfile-link navbar-left"><i class="bi bi-person-fill-gear"></i></a>

<h2 style="margin-top:30px;" class="text-center">Orders History</h2>
<p style="text-align:center;margin-top:-5px;">
    To view the order details, simply click on the order ID.
</p>

<div class="table-container">
    @if (Model.Count == 0)
    {
        <p>No orders found.</p>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>Order ID</th>
                        <th>Order Date</th>
                        <th>Order Address</th>
                        <th>Total Price</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model)
                    {
                        <tr>
                            <td>
                                <button class="order-details-button" onclick="location.href='@Url.Action("OrderDetails", "UserProfile", new { orderId = order.OrderId })'">@order.OrderId</button>

                            </td>
                            <td>@order.OrderDate.ToString("yyyy-MM-dd HH:mm:ss")</td>
                            <td>@order.Address.AddressLine, @order.Address.City, @order.Address.Governorate, @order.Address.Country</td>
                            <td>@order.TotalAmount EGP</td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
 *@